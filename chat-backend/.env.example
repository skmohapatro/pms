# Dell GenAI Authentication
# Set USE_SSO=true to use SSO authentication (requires aia_auth module)
# Set USE_SSO=false to use client credentials
USE_SSO=true

# Required when USE_SSO=false
CLIENT_ID=your-client-id
CLIENT_SECRET=your-client-secret
